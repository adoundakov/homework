<% track.notes.each do |note| %>
  <%= note.user.email %>: <%= note.body %><br>
  <% if note.user == current_user || current_user.admin %>
    <%= button_to 'Delete', track_note_url(track_id: note.track_id, id: note.id), method: :delete %>
  <% end %>
  <br>
<% end %>
