<h1>Tracks List</h1>

<% Track.all.each do |track| %>
  title: <%= track.title %> | album: <%= track.album_name %>
  <% if current_user.admin %>
    <%= button_to 'Edit', edit_track_url(track) , method: :get %>
    <%= button_to 'Delete', track_url(track) , method: :delete %>
  <% end %>
<% end %>
